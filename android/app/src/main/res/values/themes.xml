<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose 主题接管；这里仅留空 Material3 base，避免系统 status bar 闪烁 -->
    <style name="Theme.PersonalAssistant" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
